# StartupDBScraping_Docker

```$ docker-compose up -d --build```  
```$ docker-compose exec scraping bash```  

```# cd opt```  
```# git clone ```  
```# python scrape_startupDB ver.X.py```  

X is the lastest number
